{% trans_default_domain 'messages' %}

<h2>{{ 'Installation - Step {step}'|trans({'{step}': '2'}) }}</h2>
<p>{{ 'database_build_warning'|trans }}</p>
{% if message is defined and message is iterable %}
    <div class="{{ message.class }}">{{ message.message|trans }}</div>
{% endif %}

