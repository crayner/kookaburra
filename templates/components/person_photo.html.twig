{%  trans_default_domain 'messages' %}

{% if person is not same as(null) %}
    {% set photo = person.photo(size, class|default('')) %}
    <img src="{{ asset(photo.asset|replace({'{theme}': getThemeName()})) }}" class="{{ photo.class }}" />
{% endif %}
