{% deprecated 'The "default/display_image.html.twig" template is deprecated, use "components/photo.html.twig" instead.' %}
<img src="{{ asset(image.asset|replace({'{theme}': getThemeName()})) }}" class="{{ image.class }}" />